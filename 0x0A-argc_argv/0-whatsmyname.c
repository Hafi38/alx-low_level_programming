#include "main.h"
#include <stdio.h>
/**
 * main - Prints the program name
 * @argc - Counts command line arguments
 * @argv - Array name
 * Returns = 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
